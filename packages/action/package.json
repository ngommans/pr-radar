{
  "name": "pr-radar-action",
  "version": "1.0.0",
  "description": "GitHub Action — detect file-level collisions across open pull requests",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "echo \"Action tests: see self-test.yml workflow\""
  },
  "keywords": [
    "github-action",
    "pull-request",
    "collision",
    "merge-conflict"
  ],
  "author": "ngommans",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ngommans/pr-radar.git",
    "directory": "packages/action"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@octokit/plugin-throttling": "^9.0.0",
    "minimatch": "^9.0.4",
    "pr-radar-core": "*"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  },
  "engines": {
    "node": ">=20"
  }
}
